class Note {//โครงสร้างข้อมูลที่จะเก็บ ไว้เก็บโน๊ต
  String id;
  String subtitle;
  String title;
  String time;
  int image;
  bool isDon;
  Note(this.id, this.subtitle, this.time, this.image, this.title, this.isDon);
}